import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.barcodescanner',
  appName: 'barcodeScannerApp',
  webDir: 'dist/producto-scaner'

};

export default config;
