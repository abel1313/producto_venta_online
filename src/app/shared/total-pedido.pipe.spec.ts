import { TotalPedidoPipe } from './total-pedido.pipe';

describe('TotalPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
